# Use the shared backend_v2 root env (not legacy backend/):
#   ../../.env   (from a service folder)
#   ../.env      (from gateway/ or realtime/)
#
# Docker Compose: env_file: .env
# Local: ./scripts/dev-service.sh <service> <port>

PORT=8081
